Display the maximum salary being paid to Clerk
select max(salary) from employee e , job j 
where e.job = j.jcode and j.name = 'Clerk'
MAX(SALARY)
120000

display the maximum salary being paid to depart number 20
select max(salary) from employee where deptno=20MAX(SALARY)
800000

display the average salary drawn by managers
select avg(salary) 
from employee
where empno in (select distinct(mgr_no) from employee)
AVG(SALARY)
866666.667

display the total salary drawn by ANALYST working in depart number 40
select  sum(salary)  from employee e,job j 
where e.job = j.jcode and j.name = 'Analyst' and e.deptno=40
SUM(SALARY)
1000000


display the name of all the employees who are working in depart number 20

select ename  from employee where deptno=20

ENAME
Nirmala
Kumar 


no of employees in deach department that has location in hyderabad and have more than 1 employee
select count(*), d.deptno
from employee e, dept d, location l
where e.deptno = d.deptno and d.location  = l.lcode and l.name = 'Hyderabad'
group by d.deptno
having count(*) > 1

no of employees for each job name
select count(*),j.name from employee e, job j
where e.job = j.jcode
group by j.name

COUNT(*) 	NAME
2 	Manager
2 	Analyst
4 	Staff
3 	Clerk
1 	President

deptname, count of employee of those dept that have a average salary > 40000
select d.name, count(*)
from employee e, dept d
where e.deptno = d.deptno
group by d.name
having avg(salary) > 40000

NAME 	COUNT(*)
Research 	3
Sales 	5
Operations 	4



for each jobcode give total salary

select sum(salary) from employee group by job

SUM(SALARY)
1200000
1600000
2000000
320000
2100000



sum of salary for clerks for each dept

select sum(salary),e.deptno
from employee e, job j
where e.job = j.jcode and j.name='Clerk'
group by e.deptno

SUM(SALARY) 	DEPTNO
80000 		30
120000 		20
120000 		23



dislay the name of the employee who is earning highest salary
select ename from employee where salary = (select max(salary) from employee)

ENAME
Venkat 


display the employee number and name for employee working as clerk and earning highest salary among clerks

select ename, empno 
from employee
where job =  ( select jcode
                        from job 
                        where name = 'Clerk') 
            and 
            salary = (select max(salary) 
                            from employee 
                            where job =  ( select jcode
                                                    from job 
                                                    where name = 'Clerk') )


ENAME 	EMPNO
Suresh 	10
Kumar 	12

